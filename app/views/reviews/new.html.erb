<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <%= link_to("Back", restaurant_path(@restaurant)) %>
    </div>
    <div class="col-xs-4">
      <%= simple_form_for [@restaurant, @review] do |f| %>
        <%= f.input :content %>
        <%= f.input :rating %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>

